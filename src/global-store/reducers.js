import { combineReducers } from 'redux'
import actionTypes from './types'
// import { } from '../data'

/* const banks = (state = bancos, action) => {
    switch(action.type) {
        default: {
            return state
        }
    }
}

const rootReducer = combineReducers({  })

export default rootReducer */